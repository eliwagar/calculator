#include <stdio.h>
#include <stdlib.h>

typedef struct calc{
			int re;
			int im;
			}calc;

void sum(calc c1, calc c2);
void min(calc c1, calc c2);
void umn(calc c1, calc c2);
void del(calc c1, calc c2);
